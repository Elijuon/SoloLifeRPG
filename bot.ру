import telebot
from telebot import types

bot = telebot.TeleBot("ВАШ_ТОКЕН_БОТА")  # Замените на токен от @BotFather

@bot.message_handler(commands=['start'])
def start(message):
    # Создаем кнопку
    markup = types.ReplyKeyboardMarkup(resize_keyboard=True)
    button = types.KeyboardButton("🎮 Открыть игру", web_app=types.WebAppInfo(url="https://elijuon.github.io/SoloLifeRPG/"))
    markup.add(button)
    
    # Отправляем сообщение с кнопкой
    bot.send_message(
        chat_id=message.chat.id,
        text="Добро пожаловать в SoloLifeRPG! Нажмите кнопку ниже, чтобы начать.",
        reply_markup=markup
    )

bot.polling()
